你现在负责执行“阶段 3：初始化工程与开发骨架”。请不要一上来就乱建业务代码，而是先对照文档确认初始化范围，再按最小闭环完成工程搭建。

如需编写代码，必须同时遵守 `prompts/12_Cursor_代码规范与实现要求.prompt.md`。
输出结果时，请同时遵守 `prompts/13_Cursor_输出汇报格式规范.prompt.md`。
任务结束前，请执行 `prompts/14_Cursor_提交前检查与收口.prompt.md`。

## 一、必须先读取的文档

1. `docs/16_AI执行总入口与任务拆解.md`
2. `docs/09_详细开发执行步骤.md`
3. `docs/10_项目初始化执行清单.md`
4. `docs/15_单人并行开发编排与依赖关系.md`
5. `docs/14_复杂安全项后置与测试质量策略.md`

如仓库已有代码，请再检查当前目录结构、package 管理方式、现有脚本和 README。

## 二、默认前提

请默认接受这些前提：

- 前端：`Vue 3 + TypeScript + Vite + Element Plus + Pinia + Vue Router`
- 后端：`NestJS`
- 数据库：`PostgreSQL`
- 项目目录优先考虑：`frontend / backend / docs / scripts`
- 当前目标是把工程骨架搭稳，不进入具体业务模块深开发
- 必须为后续测试和并行开发预留好结构

## 三、本轮任务目标

请完成“从 0 到可启动骨架”的计划与实现。

至少覆盖：

1. 前后端目录结构
2. 基础环境变量模板
3. 前端项目初始化
4. 后端项目初始化
5. 数据库连接入口
6. 统一返回 / 分页 / 错误处理基础结构
7. 测试框架基础能力
8. 启动与验证脚本

## 四、请按以下顺序工作

### 1. 先输出初始化差距分析

请先说明：

- 仓库里目前已有些什么
- 与 `docs/10_项目初始化执行清单.md` 比，缺什么
- 哪些可以本轮完成
- 哪些要后置

### 2. 输出本轮实施计划

请拆成 4~8 个任务，每个任务写清：

- 任务目标
- 涉及目录 / 文件
- 是否可并行
- 完成标准

### 3. 再开始实现

实现时遵守：

- 只做初始化和骨架工作
- 不擅自进入客户 / 财务 / 文件等业务细节
- 公共结构尽量一次定稳
- 改动尽量小步提交式推进
- 如果需要安装依赖，先列出命令与原因

### 4. 必须补最小测试与验证

至少完成：

- 前端基础测试可运行
- 后端基础测试可运行
- 前端可启动或可构建
- 后端可启动或可通过基本检查
- 数据库连接配置路径明确

### 5. 最终输出

请总结：

- 完成了哪些初始化项
- 改了哪些文件
- 哪些命令已运行
- 测试 / 验证结果
- 当前遗留问题
- 下一轮建议

## 五、限制要求

- 不要越界开发业务页面
- 不要自己改变技术栈
- 不要忽略测试框架
- 不要把敏感配置直接写死到仓库
- 如遇文档与现状冲突，请先指出再处理

请用中文输出，并优先保证稳、清晰、可执行。
